// a twitter account will be used as the registry
// each tweet will hold the key/value or name/link to a package
// when (lkm install name) is ran the resolver will scrape the twitter looking for key and grab value
// when (lkm install url) is ran the resolver will not be used
